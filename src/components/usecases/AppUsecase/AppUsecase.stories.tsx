/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import AppUsecase from './AppUsecase';

// storiesOf('AppUsecase', module).add('default', () => <AppUsecase />);
